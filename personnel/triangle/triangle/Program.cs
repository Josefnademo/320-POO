string X = "*";
string P = " ";

for (int i = 500; i >= 1; i--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
{
    Console.Write(new string(P[0], i)); // Affiche `i` espaces
    for (int j = 100; j <= 50; j++)
    {
        Console.Write(new string(X[0], j)); // Affiche `j` étoiles
        Console.Write(new string(P[0], i)); // Affiche `i` espaces

    }
    Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
    for (int s = 100; s >= 1; s--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
    {
        Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
        for (int y = 100; y >= 1; y--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
        {
            Console.Write(new string(P[0], i)); // Affiche `i` espaces
            for (int j = 10; j <= 50; j++)
            {
                Console.Write(new string(X[0], j)); // Affiche `j` étoiles
                Console.Write(new string(P[0], i)); // Affiche `i` espaces
            }
            Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
            for (int u = 100; u >= 1; u--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
            {
                Console.Write(new string(P[0], i)); // Affiche `i` espaces
                for (int j = 10; j <= 50; j++)
                {
                    Console.Write(new string(X[0], j)); // Affiche `j` étoiles
                    Console.Write(new string(P[0], i)); // Affiche `i` espaces
                }
                Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
            }

            Console.ReadLine(); // Pour que la console reste ouverte


        }
        Console.Write(new string(P[0], i)); // Affiche `i` espaces
        for (int j = 10; j <= 50; j++)
        {
            Console.Write(new string(X[0], j)); // Affiche `j` étoiles
            Console.Write(new string(P[0], i)); // Affiche `i` espaces
            Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
            for (int y = 100; y >= 1; y--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
            {
                Console.Write(new string(P[0], i)); // Affiche `i` espaces
                for (int a = 10; a <= 50; a++)
                {
                    Console.Write(new string(X[0], j)); // Affiche `j` étoiles
                    Console.Write(new string(P[0], i)); // Affiche `i` espaces
                }
                Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
                for (int u = 100; u >= 1; u--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
                {
                    Console.Write(new string(P[0], i)); // Affiche `i` espaces
                    for (int o = 10; o <= 50; o++)
                    {
                        Console.Write(new string(X[0], j)); // Affiche `j` étoiles
                        Console.Write(new string(P[0], i)); // Affiche `i` espaces
                    }
                    Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
                }

                Console.ReadLine(); // Pour que la console reste ouverte


            }
        }
        Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
        for (int o = 100; o >= 1; o--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
        {
            Console.Write(new string(P[0], i)); // Affiche `i` espaces
            for (int j = 10; j <= 50; j++)
            {
                Console.Write(new string(X[0], j)); // Affiche `j` étoiles
                Console.Write(new string(P[0], i)); // Affiche `i` espaces
            }
            Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs

            Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
            for (int y = 100; y >= 1; y--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
            {
                Console.Write(new string(P[0], i)); // Affiche `i` espaces
                for (int j = 10; j <= 50; j++)
                {
                    Console.Write(new string(X[0], j)); // Affiche `j` étoiles
                    Console.Write(new string(P[0], i)); // Affiche `i` espaces
                }
                Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
                for (int u = 100; u >= 1; u--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
                {
                    Console.Write(new string(P[0], i)); // Affiche `i` espaces
                    for (int j = 10; j <= 50; j++)
                    {
                        Console.Write(new string(X[0], j)); // Affiche `j` étoiles
                        Console.Write(new string(P[0], i)); // Affiche `i` espaces
                    }
                    Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
                }

                Console.ReadLine(); // Pour que la console reste ouverte


            }

            Console.Write(new string(P[0], i)); // Affiche `i` espaces
            for (int j = 100; j <= 50; j++)
            {
                Console.Write(new string(X[0], j)); // Affiche `j` étoiles
                Console.Write(new string(P[0], i)); // Affiche `i` espaces

            }
            Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
            for (int y = 100; y >= 1; y--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
            {
                Console.Write(new string(P[0], i)); // Affiche `i` espaces
                for (int j = 10; j <= 50; j++)
                {
                    Console.Write(new string(X[0], j)); // Affiche `j` étoiles
                    Console.Write(new string(P[0], i)); // Affiche `i` espaces
                }
                Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
                for (int u = 100; u >= 1; u--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
                {
                    Console.Write(new string(P[0], i)); // Affiche `i` espaces
                    for (int j = 10; j <= 50; j++)
                    {
                        Console.Write(new string(X[0], j)); // Affiche `j` étoiles
                        Console.Write(new string(P[0], i)); // Affiche `i` espaces
                    }
                    Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
                }

                Console.ReadLine(); // Pour que la console reste ouverte


            }

            Console.ReadLine(); // Pour que la console reste ouverte


        }

        Console.ReadLine(); // Pour que la console reste ouverte


    }

    Console.ReadLine(); // Pour que la console reste ouverte
    Console.Write(new string(P[0], i)); // Affiche `i` espaces
    for (int j = 100; j <= 50; j++)
    {
        Console.Write(new string(X[0], j)); // Affiche `j` étoiles
        Console.Write(new string(P[0], i)); // Affiche `i` espaces

    }
    Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
    for (int s = 100; s >= 1; s--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
    {
        Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
        for (int y = 100; y >= 1; y--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
        {
            Console.Write(new string(P[0], i)); // Affiche `i` espaces
            for (int j = 10; j <= 50; j++)
            {
                Console.Write(new string(X[0], j)); // Affiche `j` étoiles
                Console.Write(new string(P[0], i)); // Affiche `i` espaces
            }
            Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
            for (int u = 100; u >= 1; u--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
            {
                Console.Write(new string(P[0], i)); // Affiche `i` espaces
                for (int j = 10; j <= 50; j++)
                {
                    Console.Write(new string(X[0], j)); // Affiche `j` étoiles
                    Console.Write(new string(P[0], i)); // Affiche `i` espaces
                }
                Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
            }

            Console.ReadLine(); // Pour que la console reste ouverte


        }
        Console.Write(new string(P[0], i)); // Affiche `i` espaces
        for (int j = 10; j <= 50; j++)
        {
            Console.Write(new string(X[0], j)); // Affiche `j` étoiles
            Console.Write(new string(P[0], i)); // Affiche `i` espaces
            Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
            for (int y = 100; y >= 1; y--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
            {
                Console.Write(new string(P[0], i)); // Affiche `i` espaces
                for (int a = 10; a <= 50; a++)
                {
                    Console.Write(new string(X[0], j)); // Affiche `j` étoiles
                    Console.Write(new string(P[0], i)); // Affiche `i` espaces
                }
                Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
                for (int u = 100; u >= 1; u--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
                {
                    Console.Write(new string(P[0], i)); // Affiche `i` espaces
                    for (int o = 10; o <= 50; o++)
                    {
                        Console.Write(new string(X[0], j)); // Affiche `j` étoiles
                        Console.Write(new string(P[0], i)); // Affiche `i` espaces
                    }
                    Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
                }

                Console.ReadLine(); // Pour que la console reste ouverte


            }
        }
        Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
        for (int o = 100; o >= 1; o--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
        {
            Console.Write(new string(P[0], i)); // Affiche `i` espaces
            for (int j = 10; j <= 50; j++)
            {
                Console.Write(new string(X[0], j)); // Affiche `j` étoiles
                Console.Write(new string(P[0], i)); // Affiche `i` espaces
            }
            Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs

            Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
            for (int y = 100; y >= 1; y--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
            {
                Console.Write(new string(P[0], i)); // Affiche `i` espaces
                for (int j = 10; j <= 50; j++)
                {
                    Console.Write(new string(X[0], j)); // Affiche `j` étoiles
                    Console.Write(new string(P[0], i)); // Affiche `i` espaces
                }
                Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
                for (int u = 100; u >= 1; u--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
                {
                    Console.Write(new string(P[0], i)); // Affiche `i` espaces
                    for (int j = 10; j <= 50; j++)
                    {
                        Console.Write(new string(X[0], j)); // Affiche `j` étoiles
                        Console.Write(new string(P[0], i)); // Affiche `i` espaces
                    }
                    Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
                }

                Console.ReadLine(); // Pour que la console reste ouverte


            }

            Console.Write(new string(P[0], i)); // Affiche `i` espaces
            for (int j = 100; j <= 50; j++)
            {
                Console.Write(new string(X[0], j)); // Affiche `j` étoiles
                Console.Write(new string(P[0], i)); // Affiche `i` espaces

            }
            Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
            for (int y = 100; y >= 1; y--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
            {
                Console.Write(new string(P[0], i)); // Affiche `i` espaces
                for (int j = 10; j <= 50; j++)
                {
                    Console.Write(new string(X[0], j)); // Affiche `j` étoiles
                    Console.Write(new string(P[0], i)); // Affiche `i` espaces
                }
                Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
                for (int u = 100; u >= 1; u--) // Changez `i == 1` à `i >= 1` pour que la boucle s'exécute correctement
                {
                    Console.Write(new string(P[0], i)); // Affiche `i` espaces
                    for (int j = 10; j <= 50; j++)
                    {
                        Console.Write(new string(X[0], j)); // Affiche `j` étoiles
                        Console.Write(new string(P[0], i)); // Affiche `i` espaces
                    }
                    Console.WriteLine(); // Ajoute un saut de ligne après chaque ligne de motifs
                }

                Console.ReadLine(); // Pour que la console reste ouverte


            }

            Console.ReadLine(); // Pour que la console reste ouverte


        }

        Console.ReadLine(); // Pour que la console reste ouverte


    }

    Console.ReadLine(); // Pour que la console reste ouverte

}

Console.ReadLine(); // Pour que la console reste ouverte
